package Var::Statistics;

use strict;

use Var::Spiffy '-base';
our @EXPORT = qw(

		 bitcount
		 charcount
		 entropy

		 );


sub bitcount {
#    my $self = shift;
#    &String::BitCount::BitCount($self->{var});
}

sub charcount {
}

sub entropy {
}

1;
